parameters:
  # WordPress driver manager.
  wordpress.wordpress.api_driver: wpcli
  wordpress.wordpress.class: PaulGibbs\WordpressBehatExtension\WordpressDriverManager

  # Context initializsr.
  wordpress.context.initialiser.class: PaulGibbs\WordpressBehatExtension\Context\Initialiser\WordpressAwareInitialiser

  # Parameters.
  wordpress.parameters: {}

services:
  wordpress.wordpress:
    class: %wordpress.wordpress.class%
    arguments:
      - {}
  wordpress.context.initialiser:
    class: %wordpress.context.initialiser.class%
    arguments:
      - "@wordpress.wordpress"
      - %wordpress.parameters%
    tags:
      - { name: context.initialiser }
